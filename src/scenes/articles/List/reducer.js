const initialState = {
  isFetching: false,
};

const articlesListReducer = (state = initialState, action) => {
  switch (action.type) {

    default:
      return state;
  }
};

export default articlesListReducer;
